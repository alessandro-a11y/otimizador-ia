services:
  - type: web
    name: otimizador-ia
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn otimizador.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
